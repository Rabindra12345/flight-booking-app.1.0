spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/flightbookingapp
    username: root
    password: password
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  security:
    user:
      name: admin
      password: admin@321!^*#
      roles: ADMIN
baldur:
  app:
      jwtSecret: ======================rabindra=Spring===========================
      jwtExpirationMs: 32000

server:
  port: 8081

